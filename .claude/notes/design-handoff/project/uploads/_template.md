---
screen: <화면 식별자 (예: signup, main-dashboard)>
version: 1.0.0
status: draft  # draft | review | approved
author: design-agent
created: YYYY-MM-DD
updated: YYYY-MM-DD
pattern: <A | B | C | D | E | F | G>  # 07-patterns.md 참조
---

# DSD · <화면 이름>

## 1. 개요

**목적**: <이 화면이 해결하는 사용자 문제 한 문장>
**주요 사용자**: <어떤 상태/단계의 사용자가 도달하는가>
**성공 지표**: <이 화면이 성공적으로 작동한다는 것을 어떻게 측정하는가>

## 2. User Journey 내 위치

```
<이전 화면> → [이 화면] → <다음 화면>
```

- **진입 경로**: <어떻게 이 화면에 도달하는가>
- **이탈 경로**: <사용자가 취할 수 있는 액션과 그 결과>

## 3. 레이아웃 패턴

- **패턴**: `<A/B/C/D/E/F/G from 07-patterns.md>`
- **선택 이유**: <왜 이 패턴이 적합한지>

## 4. 레이아웃 상세

```
<ASCII 또는 구조 설명>
```

### 영역별 상세

- **Header/Top**: <있다면 구성 요소>
- **Main/Body**: <주요 콘텐츠 영역>
- **Sidebar/Right**: <보조 영역>
- **Footer/Bottom**: <있다면 하단 요소>

## 5. 컴포넌트 인벤토리

### 사용할 기존 컴포넌트 (재사용)

| 컴포넌트 | Variant | 용도 |
|----------|---------|------|
| Button | primary | 주요 CTA |
| Input | - | 이메일 입력 |
| ... | | |

### 신규 필요 컴포넌트 (명확히 justify)

| 컴포넌트 | 이유 | 승인 필요 여부 |
|----------|------|---------------|
| (없음) | | |

## 6. 디자인 토큰 적용

- **배경**: `--bg` (페이지) / `--bg-elev-1` (카드)
- **주요 텍스트**: `--text`
- **보조 텍스트**: `--text-2` / `--text-3`
- **포인트 컬러**: `--brand` (주요 CTA, 활성 상태에만)
- **간격**: <주요 간격 기록>
- **Radius**: <사용한 radius 기록>

## 7. 타이포그래피 적용

| 요소 | 스타일 | 색상 |
|------|--------|------|
| 페이지 제목 | `display` | `--text` |
| 설명 | `body` | `--text-2` |
| 라벨 | `ui-label` | `--text` |
| 메타 | `mono-meta` | `--text-3` |

## 8. 아이콘

| 위치 | 아이콘 (Lucide) | 크기 | 색상 |
|------|----------------|------|------|
| 예시: 제출 버튼 | `ArrowRight` | 16 | currentColor |

## 9. 인터랙션

### 사용자 액션 → 시스템 반응

| 액션 | 반응 | 피드백 |
|------|------|--------|
| Primary 버튼 클릭 | API 호출 | 버튼에 로딩 상태, 성공 시 다음 화면 |
| ... | | |

### 키보드 네비게이션

- `Tab` 순서: <명시적으로 정의>
- `Enter`: <기본 액션>
- `Escape`: <모달이면 닫기>

### 로딩/에러 상태

- **로딩**: <어떻게 표시하는가 — 스켈레톤? 스피너? 버튼 상태?>
- **에러**: <어디에 어떤 메시지를 어떻게 표시하는가>
- **빈 상태**: <데이터가 없을 때 어떻게 표시하는가>

## 10. 반응형

### Mobile (<640px)

<모바일에서 레이아웃이 어떻게 바뀌는가>

### Tablet (640-860px)

<있다면>

### Desktop (>860px)

<기본 레이아웃>

## 11. 접근성

- **ARIA 라벨**: <필요한 모든 `aria-label`, `aria-describedby` 등>
- **포커스 관리**: <모달 진입 시 포커스 이동, 닫을 때 복귀 등>
- **스크린 리더 대응**: <필요한 `aria-live`, `role` 등>
- **색상 의존 정보**: <색상만으로 정보를 전달하는 곳이 있다면 대체 표기>

## 12. 카피라이팅

### 화면 내 모든 텍스트 (확정)

| 위치 | 텍스트 |
|------|--------|
| 페이지 제목 | <확정된 한국어 텍스트> |
| 설명 | <확정된 한국어 텍스트> |
| Primary CTA | <확정된 버튼 라벨> |
| 에러 메시지 | <모든 가능한 에러 메시지> |

**규칙**: 01-foundations.md의 Tone of Voice를 따를 것.

## 13. 상태 머신 (복잡한 경우)

```
initial → loading → success/error
error → retry → loading
```

## 14. API / 데이터 의존성

| API | 메서드 | 엔드포인트 | 언제 호출 |
|-----|--------|-----------|-----------|
| (예) submitSignup | POST | `/auth/signup` | 폼 제출 시 |

### 데이터 스키마 (필요 시)

```ts
interface FormData {
  email: string;
  nickname: string;
  password: string;
}
```

## 15. 엣지 케이스

- **네트워크 오프라인**: <처리 방법>
- **API 타임아웃**: <처리 방법>
- **중복 제출**: <처리 방법>
- **브라우저 뒤로가기**: <처리 방법>

## 16. 참조 자료

- 참조 화면: `reference/avating-design-v2.html` 의 <섹션 번호>
- 관련 DSD: <다른 DSD 링크>
- 디자인 시스템 문서: <참조한 섹션들>

---

## Design Rationale (부록)

디자인 결정의 **Why**를 기록. 이 섹션은 리뷰어가 "왜 이렇게 했는가"를 이해하는 데 쓴다.

### 결정 1: <결정 내용>

- **이유**: <왜 이렇게 했는가>
- **대안**: <고려했으나 채택하지 않은 대안과 그 이유>

### 결정 2: ...

---

## Revision History

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0.0 | YYYY-MM-DD | design-agent | 초기 작성 |
